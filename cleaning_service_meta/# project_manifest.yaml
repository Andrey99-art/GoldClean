manifest_version: "4.2-STABLE"
last_updated: "2025-12-03"

project_metadata:
  status: "PRODUCTION (HARDENED)"
  git_repo: "github.com/Andrey99-art/goldclean"
  
infrastructure_summary:
  app: "Django 4.2 + Gunicorn (3 workers)"
  db: "PostgreSQL 15 Alpine"
  storage: "Docker Named Volume (pg_data)"
  protection: "Auto-create database in entrypoint"

operational_rules:
  deploy: "git pull && docker compose up -d --build"
  backup: "docker compose exec web python manage.py dumpdata > backup_$(date +%Y%m%d).json"
  logs: "docker compose logs -f web"
  danger_zone: "NEVER run 'docker compose down -v'"

resolved_issues:
  - date: "2025-12-03"
    problem: "goldclean_db doesn't exist (corrupted volume)"
    solution: "Volume recreated, entrypoint hardened with auto-create logic"

next_steps:
  - "Set up regular backups (cron + dumpdata)"
  - "Consider adding Nginx reverse proxy for production"
  - "Monitor logs periodically"